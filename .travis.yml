language: python
python:
    - "2.7"
    - "3.3"
install: 
    - "pip install -r requirements.txt --use-mirrors"
    - "pip install coveralls --use-mirrors"
script:
    - chmod -R 777 test.sh
    - ./test.sh
after_success: "coveralls"
notifications:
  email: false